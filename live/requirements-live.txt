# Live Streaming Dependencies
# Mangoma Live - YouTube RTMP via Google Lyria

# Note: This project uses npm (package.json), not pip
# This file is informational only to list tools needed

# System dependencies (install via apt/yum/brew):
# - ffmpeg (required for RTMP streaming)
# - nodejs + npm (required to run the server)

# After installing system deps:
# npm install
